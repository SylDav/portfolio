---
const { title, description, tech, link } = Astro.props;
---
<div class="card">
    <h3 class="text-xl font-semibold text-gray-100 mb-2">{title}</h3>
    <p class="text-gray-400 text-sm mb-4">{description}</p>
    <p class="text-gray-500 text-xs mb-4">{tech}</p>
    <a href={link} target="_blank" class="text-orange-400 text-sm hover:underline">Voir →</a>
</div>