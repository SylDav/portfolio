---
import { Image } from 'astro:assets';
import meImage from '../images/me.jpeg';
---

<section id="hero" class="min-h-[90vh] flex items-center pt-8 md:pt-12 pb-16 md:pb-20">
  <div class="grid md:grid-cols-2 items-center gap-12 w-full">

    <!-- IMAGE -->
    <div class="flex justify-center order-2 md:order-1">
      <div class="relative aspect-square w-48 md:w-64">
        <Image 
          src={meImage} 
          alt="Photo de Sylvain DAVID, développeur Web Backend PHP"
          width={256}
          height={256}
          class="w-48 h-48 md:w-64 md:h-64 rounded-2xl object-cover border-4 shadow-2xl"
          style="border-color: var(--accent);"
          loading="lazy"
          sizes="(max-width: 768px) 192px, 256px"
        />
        <!-- Badge de statut -->
        <div class="absolute -bottom-4 left-1/2 transform -translate-x-1/2 px-4 py-2 rounded-full text-sm font-semibold shadow-lg whitespace-nowrap" style="background-color: #047857; color: white;">
          À l'écoute d'opportunités CDI
        </div>
      </div>
    </div>

    <!-- TEXT -->
    <div class="order-1 md:order-2 text-center md:text-left">
      <!-- Badge spécialisation -->
      <div class="inline-flex items-center bg-accent/10 border border-accent/30 rounded-full px-4 py-2 mb-6">
        <span class="text-accent font-medium">Développeur Web Backend PHP</span>
      </div>
      
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 leading-tight">
        Salut, moi c'est <span class="text-accent">Sylvain</span> 👋
      </h1>

      <p class="text-muted text-lg md:text-xl leading-relaxed max-w-2xl mb-8">
        Développeur Web Backend PHP avec <span class="font-semibold text-text-main">5 ans d'expérience</span> en 
        <span class="font-semibold text-text-main">PHP, Laravel, Symfony</span> et <span class="font-semibold text-text-main">Vue.js</span>. 
        <span class="font-semibold text-text-main">Autodidacte</span> et <span class="font-semibold text-text-main">très adaptable</span>, je cherche à mettre mes compétences au service de domaines qui ont un <span class="font-semibold text-text-main">impact positif</span> comme la <span class="font-semibold text-text-main">santé</span> ou l'<span class="font-semibold text-text-main">aide aux personnes</span>.
      </p>
      
      <!-- Skills tags -->
      <div class="flex flex-wrap gap-2 mb-8 justify-center md:justify-start">
        <span class="px-3 py-1 bg-card border border-border rounded-full text-sm">PHP</span>
        <span class="px-3 py-1 bg-card border border-border rounded-full text-sm">Laravel</span>
        <span class="px-3 py-1 bg-card border border-border rounded-full text-sm">Symfony</span>
        <span class="px-3 py-1 bg-card border border-border rounded-full text-sm">Vue.js</span>
        <span class="px-3 py-1 bg-card border border-border rounded-full text-sm">Docker</span>
        <span class="px-3 py-1 bg-card border border-border rounded-full text-sm">MySQL</span>
        <span class="px-3 py-1 bg-card border border-border rounded-full text-sm">IA</span>
        <span class="px-3 py-1 bg-card border border-border rounded-full text-sm">Git</span>
      </div>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center md:justify-start">
        <a href="#contact" class="btn-primary inline-flex items-center justify-center gap-2 hover:scale-105 transition-transform" aria-label="Me contacter par email">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
          </svg>
          Me contacter
        </a>
        <a href="#experience" class="inline-flex items-center justify-center gap-2 px-6 py-3 border border-border rounded-lg hover:bg-card transition-colors" aria-label="Voir mon parcours professionnel">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
          </svg>
          Mon parcours
        </a>
      </div>
    </div>

  </div>
</section>